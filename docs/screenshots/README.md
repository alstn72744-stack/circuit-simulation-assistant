# Screenshot Plan

현재 이 폴더에는 공개 검토를 마친 이미지가 없습니다. 가짜 화면이나 결과 이미지를 만들지 않았습니다. 아래 이름은 향후 실제 앱에서 확보할 screenshot 계획이며 아직 존재하는 파일의 링크가 아닙니다.

| 추천 파일명 | 보여줄 내용 |
| --- | --- |
| `01_problem_or_initial_ui.png` | 최초 요청 / 검토 화면 |
| `02_ac_result.png` | AC gain / bandwidth / graph |
| `03_parameter_sweep.png` | 비교 표, metric graph, point evidence |
| `04_transient_result.png` | Vpp gain과 waveform |
| `05_dc_result.png` | DC sweep와 requested-point card |
| `06_analysis_summary.png` | measured / derived facts / warnings |
| `07_trace_suggestion.png` | V(out) vs V(vout) 추천과 사용자 선택 |

프로젝트 root의 기존 `screenshots/`에는 초기 UI 이미지 4개가 있고, `simulation_output/`에는 graph와 browser 검증 이미지가 있습니다. 원본은 보존하되 두 위치 모두 공개 제외했습니다. 게시할 이미지는 직접 열어 사용자명·전체 경로·API 설정·회로 배포 권한을 확인하고 필요한 부분을 가린 사본만 여기에 넣습니다. 현재 회귀 상태를 보여주는지, 예전 UI인지도 caption에 표시합니다. 실제 측정값을 꾸미거나 바꾸지 않습니다.
